#include <stdio.h>
#include <string.h>
int main()
{
	int a = 0;
	int b = 0;
	int c = 0;
	printf("输入的两个数为:");
		scanf("%d%d", &a, &b);
		while (c=a%b)
		{
			a = b;
			b = c;
		}
		printf("最大公约数为:%d\n", b);
		return 0;
}
