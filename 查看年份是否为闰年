#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
#include<string.h>
int main()
{
	int a=1900;
	getchar(a);
	if (a % 4 == 0 && a % 100 != 0)
		printf("是闰年\n");
	else if (a % 100 == 0 && a % 400 == 0)
		printf("是闰年\n");
	else
		printf("不是闰年\n");
	return 0;
}
